global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'workplaceiq'
    scrape_interval: 10s
    static_configs:
      - targets: ['workplaceiq-web:4792']
        labels:
          app: 'workplaceiq-web'
